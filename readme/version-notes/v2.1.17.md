
## v2.1.17

---

### Dashboard — Live view me hidden widget gap

```bash
   Problem:
      Jis widget ka user ko access nahi tha, wo hide ho jata tha — par grid me uski jagah khali reh jati thi.
      Screen par beech me white space dikhta tha; neeche ke sections upar shift nahi hote the.

   Cause:
      Backend sirf allowed widgets bhejta hai.
      Frontend unhe purani x, y position par render karta tha, isliye hidden rows empty reh jati thi.

   Fix:
      Live dashboard (readOnly) par ab visible widgets vertically compact hote hain.
      compactLayoutVertically() hidden widgets ki jagah nahi chhodta.
      Builder / Super Admin view pehle jaisa hai.

   File: frontend/src/features/dashboard-builder/components/DashboardBuilder.js
```

---

### Schedule Planning — Default report par galat filters

```bash
   Problem:
      Default report par month/date fields disabled the, par API ko phir bhi fromDate, toDate, month ja rahe the.
      Isse galat data aa sakta tha.

   Fix:
      Default report: UI par current month dikhta hai, dates blank rehti hain.
      API ko sirf reportType + status bheja jata hai.
      Backend current month IMS ko bhejta hai (requestedData: "schdule").
      Custom report: sirf user jo month/date select kare, wahi jata hai.

   Files: schedule-planning/Page.js, schedulePlanService.js
```

---

### Schedule Planning — Shortage submit

```bash
   Added:
      Shortage create karte waqt user_name bhi save hota hai (backend logged-in user se, frontend se nahi).
      Backend me submitScheduleShortage add — IMS call with requestedData: "shortage".

   Note:
      Controller abhi test mode me hai. Service ready hai; live par jaane se pehle IMS call uncomment karna hoga.

   Files: schedulePlanning.controller.js, schedulePlanService.js
```

---

### Store Out — Draft save OK, submit par access error

```bash
   Problem:
      User boxes scan karke draft save kar sakta tha, par wapas aake submit par "no access" aata tha.
      Admin ne permissions change nahi kiye the — ye code change se aaya.

   Cause:
      1. fieldsForForwardingNotes sirf view allow karta tha; Store Out form ko add/edit chahiye hota hai.
      2. Draft save = create API (Add permission). Submit = update API — pehle sirf Edit allow tha.

   Fix:
      fieldsForForwardingNotes — out_entry aur forwarding_note_master se view + add + edit + authorize allow.
      outEntry.route.js — update route par add bhi allow, taaki user apna draft complete kar sake.

   Files: helperViews.js, outEntry.route.js
```

---
