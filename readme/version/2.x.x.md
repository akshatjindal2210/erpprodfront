# 2.x.x — Task

|       |               |
|-------|---------------|
| UI    | `/task/`      |
| API   | `/api/task/`  |
| Gate  | `app_task`    |

**Covers:** tasks, recurring, CL master/instances, verification, reports, red ticket, category, holiday, logs.

Deletes are hard (no soft delete).

Docs: [task.md](../module-flow/task/task.md)
