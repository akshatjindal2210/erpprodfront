"use client";

/**
 * List span is set from login / `useSyncAuthSession` (`default_list_view_span_days` on user payload).
 * This component is kept for layout compatibility; no public app-config API call.
 */
export default function ListViewSpanBootstrap() {
  return null;
}
